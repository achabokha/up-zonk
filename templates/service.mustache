import { Injectable } from '@angular/core';
import { HttpClient } from '@angular/common/http';
import { Observable } from 'rxjs';
import { {{pascalName}} } from '@models/{{kebabName}}.interface';

@Injectable({
    providedIn: 'root',
})
export class {{pascalName}}Service {
    constructor(private httpClient: HttpClient) {}

    getAll(): Observable<{{pascalName}}[]> {
        return this.httpClient.get<{{pascalName}}[]>('rest/{{pluralName}}');
    }

    create(item: {{pascalName}}) {
        return this.httpClient.post('rest/{{pluralName}}', item);
    }

    read(id): Observable<{{pascalName}}>  {
        return this.httpClient.get<{{pascalName}}>('rest/{{pluralName}}/${id}');
    }

    update(id, item: {{pascalName}}) {
        return this.httpClient.put('rest/{{pluralName}}/${id}', item);
    }

    delete(id) {
        return this.httpClient.delete('rest/{{pluralName}}/${id}');
    }
}
